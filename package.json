{
  "name": "ivr-tiagora",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "prebuild": "node install.js",
    "build": "node build.js --",
    "deploy": "node deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^3.2.2"
  }
}
